#include "vector.h"
#include <stdio.h>

int main()
{
    // Vector v = VECTOR_create();
    // Vector result;
    // printf("Initial vector: (%f, %f, %f)\n", v.x, v.y, v.z);

    // VECTOR_set(&v, 1.0f, 2.0f, 3.0f);
    // printf("After setting values: (%f, %f, %f)\n", v.x, v.y, v.z);

    // float magnitude = VECTOR_magnitude(v);
    // printf("Magnitude of vector: %f\n", magnitude);

    // VECTOR_destroy(&v);
    // printf("After destruction: (%f, %f, %f)\n", v.x, v.y, v.z);
    // Vector v1 = VECTOR_create();
    // // Vector v2 = VECTOR_create();
    // float dot_product = VECTOR_dotProduct(v1, v2);
    // printf("%.2f dot product", dot_product);
    

    return 0;
}